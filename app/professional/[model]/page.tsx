import{notFound}from"next/navigation";import Link from"next/link";import Gallery from"../Gallery";import styles from"../professional.module.css";import{getTricycle,tricycles}from"../products";
export function generateStaticParams(){return tricycles.map(item=>({model:item.slug}))}
export default async function TricyclePage({params}:{params:Promise<{model:string}>}){const{model}=await params;const item=getTricycle(model);if(!item)notFound();const specs=[[item.bed,"Dimensions de benne"],[item.capacity,"Charge utile"],[item.motor,"Motorisation"],[item.range,"Autonomie annoncée"],[item.speed,"Vitesse selon version"],[item.dimensions,"Dimensions du véhicule"]];return <main className={styles.page}><section className={styles.detailHero}><Link className={styles.back} href="/professional">← Retour à la gamme</Link><div className={styles.detailIntro}><div><span className={styles.eyebrow}>NEODRIVE PRO · {item.label}</span><h1>{item.name}</h1><h2>{item.promise}</h2></div><div><p>{item.description}</p><div className={styles.pills}>{item.features.map(x=><span key={x}>✓ {x}</span>)}</div></div></div></section><section className={styles.galleryWrap}><Gallery name={item.name} images={item.images} video={item.video} color={item.color}/></section><section className={styles.specs}>{specs.map(([value,label])=><article className={styles.spec} key={label}><b>{value}</b><span>{label}</span></article>)}</section><section className={styles.strength}><div className={styles.strengthVisual}><img src={item.images[Math.min(2,item.images.length-1)]} alt={`Benne en acier du ${item.name}`}/></div><div><span className={styles.eyebrow}>UNE VRAIE BENNE DE TRAVAIL</span><h2>Grande, accessible et construite en acier.</h2><p>Les ridelles facilitent le chargement des objets encombrants. Le format trois roues reste facile à manœuvrer dans les cours, allées, exploitations et zones de travail où un utilitaire classique prend trop de place.</p><div className={styles.checks}>{item.features.map(x=><span key={x}>✓ {x}</span>)}</div></div></section><section className={styles.useSection}><span className={styles.eyebrow}>À CHACUN SON TERRAIN</span><h2>Le {item.name} en situation.</h2><div className={styles.useGrid}>{item.uses.map((use,i)=><article key={use}><b>0{i+1}</b><h3>{use}</h3></article>)}</div></section><section className={styles.cta}><div><span className={styles.eyebrow}>BESOIN D’UN CONSEIL ?</span><h2>Parlons de votre usage.</h2><p>Indiquez-nous votre charge, votre terrain et vos distances quotidiennes.</p></div><a href={`https://wa.me/33628261446?text=Bonjour%2C%20je%20souhaite%20des%20informations%20sur%20le%20${encodeURIComponent(item.name)}`} target="_blank" rel="noreferrer">Demander le tarif sur WhatsApp</a></section><p className={styles.legal}>*Données fabricant susceptibles de varier selon configuration et homologation.</p></main>}
