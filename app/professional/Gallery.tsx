"use client";
import{useState}from"react";import styles from"./professional.module.css";
export default function Gallery({name,images,video,color}:{name:string;images:string[];video:string;color:string}){const[index,setIndex]=useState(0);const[watching,setWatching]=useState(false);const move=(d:number)=>setIndex((index+d+images.length)%images.length);return <div className={styles.gallery} style={{"--accent":color}as React.CSSProperties}><div className={styles.stage}>{watching?<video src={video} controls autoPlay playsInline/>:<img src={images[index]} alt={`${name} — vue ${index+1}`}/>} {!watching&&<><button className={`${styles.arrow} ${styles.left}`} onClick={()=>move(-1)} aria-label="Photo précédente">‹</button><button className={`${styles.arrow} ${styles.right}`} onClick={()=>move(1)} aria-label="Photo suivante">›</button></>}<span className={styles.counter}>{watching?"Vidéo":`${index+1} / ${images.length}`}</span><button className={styles.videoButton} onClick={()=>setWatching(!watching)}>{watching?"Retour aux photos":"Voir la vidéo"}</button></div><div className={styles.thumbs}>{images.map((image,i)=><button key={image} className={`${styles.thumb} ${!watching&&i===index?styles.selected:""}`} onClick={()=>{setIndex(i);setWatching(false)}} aria-label={`Afficher la vue ${i+1}`}><img src={image} alt=""/></button>)}</div></div>}
